tags:
  - Contacts
summary: Replace contact
operationId: replaceContact
description: This endpoint fully replaces the contact with a new payload.
security:
  - bearerAuth: []
parameters:
  - in: path
    name: contactId
    required: true
    schema:
      type: string
      example: '65ca67e7ae7f10c88b598384'
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        required:
          - name
          - email
          - phoneNumber
        properties:
          name:
            type: string
            description: Contact name
            example: 'John Doe'
          email:
            type: string
            description: Contact email
            example: 'john.doe@example.com'
          phoneNumber:
            type: string
            description: Contact phone number
            example: '+380000087807'
          isFavourite:
            type: boolean
            description: Favourite contact true/false
            default: false
          contactType:
            type: string
            enum: ['work', 'home', 'personal']
            description: Type of contact
            example: 'work'
          photo:
            type: string
            format: binary
            description: Contact photo (optional)
responses:
  '200':
    description: Replace the contact
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully replaced a contact
            data:
              $ref: ../../../components/schemas/contact.yaml
  '400':
    $ref: '../../../components/responses/400.yaml'
  '401':
    $ref: '../../../components/responses/401.yaml'
  '404':
    $ref: '../../../components/responses/404.yaml'
  '500':
    $ref: '../../../components/responses/500.yaml'
